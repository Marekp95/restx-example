rootProject.name = 'rest'

def modules = [
        'model',
        'rest-api',
        'storage',
]

modules.each {
    include ":$it"
}

modules.each {
    project(":$it").projectDir = "$rootDir/$it" as File
}
